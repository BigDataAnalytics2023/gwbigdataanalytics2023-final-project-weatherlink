# Steps for our data ingestion using Spark and Docker Compose

# Here is the apache spark container
https://hub.docker.com/r/apache/spark

# Run the docker compose yaml to set every thing
`docker-compose`